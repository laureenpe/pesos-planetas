
/*function calculo() {
    var peso, planetaMarte, planetaJupiter, peso_final, nombre; //variables necesitadas para el calculo
    peso = document.getElementById("peso").value; //recogemos los datos mediante el id
    planetaMarte = document.getElementById("marte").value;
    planetaJupiter = document.getElementById("jupiter").value;

    var g_tierra = 9.8;
    var g_marte = 3.7;
    var g_jupiter = 24.8;
    
    if (planetaMarte) {
        peso_final = peso * g_marte / g_tierra;
       // nombre = "Marte";
    }
    else if (planetaJupiter) {
        peso_final = peso * g_jupiter / g_tierra;
       // nombre = "Jupiter";
    }
    else {
        peso_final = 1000000;
      //  nombre = "Chile";
    }
    peso_final = parseInt(peso_final);
   // document.getElementById("nombre").innerHTML = "peso_final";
   console.log("Tu peso en " + nombre + " es : " + peso_final + " kilos"); //it should work
}
calculo()*/